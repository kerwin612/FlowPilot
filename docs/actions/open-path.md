# 打开路径（OpenPath）

- 类型：`action`
- 场景：快速打开文件/文件夹、跳转到构建产物或日志目录。

## 配置
- `path`（必填）：路径模板，支持 `{{ }}` 变量。

## 模板示例
- `{{executors[0].result.value.outputPath}}`
- `{{envs.HOME}}/Downloads`

## 交互
- 配置面板支持“选文件/选文件夹”按钮快速填入路径。

## 行为
- 解析模板后调用系统默认方式打开文件或目录。

